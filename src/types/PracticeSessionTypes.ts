export type PracticeSessionType = {
  userId: number;
  jobTitle: string;
};

export type AnswerDetailsType = {
  question: string;
  answer: string;
  score: number;
  feedback: string;
  sampleAnswer: string;
};
